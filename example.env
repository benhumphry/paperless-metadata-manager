# Paperless Metadata Manager Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Base URL of your Paperless-ngx instance (no trailing slash)
# Examples:
#   PAPERLESS_URL=http://localhost:8000
#   PAPERLESS_URL=https://paperless.example.com
#   PAPERLESS_URL=http://192.168.1.100:8000
PAPERLESS_URL=http://localhost:8000

# API token for authentication
# Get this from: Settings → Administration → API Tokens in Paperless-ngx
# Or create via CLI: docker exec paperless document_create_token <username>
PAPERLESS_API_TOKEN=your_api_token_here

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Port for the web UI (default: 8000)
PORT=8000

# Log level: debug, info, warning, error (default: info)
LOG_LEVEL=info

# Patterns to exclude from deletion suggestions (comma-separated, case-insensitive)
# Items matching these patterns will never be suggested for deletion even if they have 0-1 documents
# Supports regex patterns - examples:
#   Simple: new,inbox,todo,review
#   Regex: ^important.*,^keep-.*,archived-\d+
#   Mixed: new,inbox,^project-.*,review
EXCLUDE_PATTERNS=new,inbox,todo,review

# =============================================================================
# LLM SETTINGS (OPTIONAL)
# =============================================================================
# Configure an LLM for AI-powered semantic grouping suggestions.
# When enabled, an "AI Group" button appears in the Merge tab.

# LLM provider type: "openai", "anthropic", or "ollama"
# LLM_TYPE=openai

# API URL (optional for OpenAI/Anthropic, required for Ollama)
# OpenAI default: https://api.openai.com/v1
# Anthropic default: https://api.anthropic.com
# Ollama default: http://localhost:11434
# LLM_API_URL=http://localhost:11434

# API token (required for OpenAI/Anthropic, not needed for Ollama)
# LLM_API_TOKEN=sk-...

# Model name (optional, sensible defaults are used)
# OpenAI: gpt-5-mini (default), gpt-4o, gpt-3.5-turbo
# Anthropic: claude-3-haiku-20240307 (default), claude-3-sonnet-20240229
# Ollama: llama3 (default), mistral, etc.
# LLM_MODEL=gpt-5-mini

# Language for LLM responses (default: English)
# Use this to ensure the LLM responds in your preferred language
# LLM_LANGUAGE=English

# Custom prompt template (optional, advanced users)
# Override the default prompt for better results with specific models.
# Available variables: {language}, {item_type}, {item_type_upper}, {items}
# Example:
# LLM_PROMPT=Analyze these {item_type_upper} and group similar ones. Respond in {language} with JSON: {{"groups": {{"GroupName": ["item1", "item2"]}}}}. Items:\n{items}
